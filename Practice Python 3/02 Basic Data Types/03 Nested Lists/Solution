import operator
if __name__ == '__main__':
    d = {}
    for _ in range(int(input())):
        name = input()
        score = float(input())
        d[name] = score

    d = sorted(d.items(), key = operator.itemgetter(1))
    c = 0
    menornt = list(d.keys())[0]
    l = [] 

    for nm, nt in d:
        if c == 0:
            menornt = nt
            c = 1
        if nt == menornt 
            l.append(nm)
    l.sort()
    for n in l:
        print(n)
