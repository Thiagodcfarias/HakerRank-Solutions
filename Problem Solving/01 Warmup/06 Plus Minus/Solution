def plusMinus(arr):
    p,n,z = 0,0,0
    for i in range(len(arr)):
        if arr[i] > 0:
            p += 1
        elif arr[i] == 0:
            z +=1
        else:
            n += 1
    print(p/len(arr),n/len(arr),z/len(arr), sep ='\n')
